package ut.distcomp.paxos;
/**
 * Client which attempts to add a message to the chat log.
 * Should broadcast a request to all replicas.
 * Update its local chat log from responses from all servers.
 * Should add a unique identifier to all the messages it sends to the replicas.
 * 
 */
public class Client {

}
